/**
 * Client-namespace projection of the last-turn-outcome domain: a pure
 * re-export of the package's types outlet, so client aggregates import only
 * the client namespace while sharing the single-source key declaration.
 *
 * @module @deepseek-ai/dsh-session-last-turn-outcome/client
 */

export type * from './types.ts'
